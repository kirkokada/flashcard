<% provide(:title, "New Card") %>

<h1>New card</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for @card do |f| %>
			<%= render 'form', f:f %>

			<%= f.submit "Save card", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>