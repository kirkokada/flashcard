<% provide(:title, "Edit Card") %>

<h1>Edit Card</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for @card do |f| %>
			<%= render 'form', f:f %>

			<%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>