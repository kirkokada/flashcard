- provide(:title, 'Edit user')
%h1 Update your profile
.row
  .span6.offset3
    = form_for @user do |f|
      = render 'form', f:f
      = f.submit 'Save changes', class: "btn btn-large btn-primary"
